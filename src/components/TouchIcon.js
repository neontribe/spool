import React, { Component } from 'react';

import styles from './css/TouchIcon.module.css';

export default class TouchIcon extends Component {
    render () {
        return (
            <span className={styles.icon}></span>
        );
    }
};
